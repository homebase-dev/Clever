.page-header
  %h3
    =@question.question_context.category.name
    %small Test
    /%small #{@question_number+1}/#{@test.questions.count}
.row
  .col-lg-12
    %p 
      =preview_html_field(@question.question_context.category.description)

.hspacer-4x

.row
  .col-lg-12
    %p.text-center
      = link_to "Los gehts,<br />auf zur ersten Frage!".html_safe, test_step_path(:id => @test.id, :assignation_number => 1), :class => 'btn btn-success', data: { no_turbolink: true }
      
.hspacer-4x