%script{:src => "https://maps.googleapis.com/maps/api/js"}
:javascript
  function initialize() {
    var positionLatLng = new google.maps.LatLng(#{t 'medAT.address.lat'}, #{t 'medAT.address.lng'});

    var mapCanvas = document.getElementById('map-canvas');
    var mapOptions = {
      center: positionLatLng, //44.5403, -78.5463 
      zoom: 16,
      mapTypeId: google.maps.MapTypeId.ROADMAP
    }
    var map = new google.maps.Map(mapCanvas, mapOptions)

    var marker = new google.maps.Marker({
        position: positionLatLng,
        map: map,
        title: '#{t 'medAT.address.owner'}'
    });

  }
  google.maps.event.addDomListener(window, 'load', initialize);



.page-header
  %h1
    Kontakt
    / <small>Subtext for header</small>
.row
  .col-lg-2
    %a.thumbnail.category{:href => "#"}
      = image_tag("placeholder_icon.png", alt: "Kontakt icon")
      %p.text-center
  .col-lg-10
    %address
      %strong=t 'medAT.address.owner'
      %br/
      =t 'medAT.address.street'
      %br/
      #{t 'medAT.address.zip'} #{t 'medAT.address.city'}
      %br/
      =t 'medAT.address.country'
      %br/
    %address
      %abbr{:title => "Phone"} Tel.
      =t 'medAT.address.phone'
      %br/
      %abbr{:title => "Phone"} E-mail
      %a{:href => "mailto:#"}=t 'medAT.address.email'
      %br/
     
.row
  .col-lg-2
    / <a href="#" class="thumbnail category"><img src="res/img/location.png" alt="..."><p class="text-center"></p></a>
  .col-lg-10
    #map-canvas.thumbnail{:style => "height: 40em"}